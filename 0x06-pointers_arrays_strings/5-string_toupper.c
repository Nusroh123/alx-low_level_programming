#include "main.h"
#include <string.h>

/**
 * string_toupper -  changes all lowercase letters to uppercase
 * @*: pointer
 * Return: a character
 */
char *string_toupper(char *)
{
	int i = 0;

	while (*(ch + i) != '\0')
	{
		if (*(ch + i) >= 90 && *(ch + i) <= 122;)
			*(ch + i) = *(ch + i) - '';
		i++;
	}
	return (ch);
}
